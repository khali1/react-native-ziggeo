/**
 * @providesModule ReactNativeZiggeoTest
 * @flow
 */
'use strict';

var ReactNativeZiggeo = require('NativeModules').ReactNativeZiggeo;

/**
 * High-level docs for the Sample iOS API can be written here.
 */

var ReactNativeZiggeoTest = {
  test: function() {
    NativeSample.test();
  }
};

module.exports = ReactNativeZiggeoTest;
